Task 6: Convoy Schema, Join Table, Migrations, Context, and Tests
=================================================================
Date: 2026-03-11
Status: COMPLETE

## Files Created

### Migrations
- backend/priv/repo/migrations/20260311180831_create_convoys.exs
  - convoys table with binary_id PK, cs_id (unique), name, description, status, timestamps
- backend/priv/repo/migrations/20260311180834_create_convoy_tasks.exs
  - convoy_tasks join table (no timestamps - required for many_to_many string join_through)
  - FK references to convoys and tasks with on_delete: :delete_all
  - unique_index on (convoy_id, task_id), index on task_id

### Schema
- backend/lib/backend/convoys/convoy.ex
  - Ecto schema with binary_id PK
  - many_to_many :tasks via "convoy_tasks" join table
  - changeset validates name (required), status (required, inclusion in active/completed/paused)
  - generate_cs_id/0 produces "cs-cv-" + 5 alphanumeric chars

### Context
- backend/lib/backend/convoys.ex
  - list_convoys/1 with optional status filter
  - get_convoy!/1 with tasks preloaded
  - get_convoy_by_cs_id!/1 with tasks preloaded
  - create_convoy/1 with auto-generated cs_id
  - update_convoy_status/2
  - delete_convoy/1
  - add_tasks_to_convoy/2 (deduplicates tasks)
  - get_convoy_progress/1 (computed, not stored)

### Tests
- backend/test/backend/convoys_test.exs
  - 26 tests covering all context functions and schema validations

## Test Results

MIX_ENV=test mix test test/backend/convoys_test.exs:
  26 tests, 0 failures

MIX_ENV=test mix test (full suite):
  92 tests, 0 failures

## Key Fix Applied
The convoy_tasks join table originally had timestamps() which caused NOT NULL violations
when Ecto's many_to_many (string join_through) tried to insert without timestamps.
Fixed by removing timestamps() from the join table migration.

## Git Commit
53fe92d feat(backend): add convoy schema with join table and tests
(convoy schema/context/migrations were in prior commit c57c027)
