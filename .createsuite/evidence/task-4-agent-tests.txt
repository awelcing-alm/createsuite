Task 4: Agent Ecto Schema, Migration, Context, and Tests
=========================================================
Date: 2026-03-11
Status: COMPLETE

Files Created:
- backend/priv/repo/migrations/20260311175938_create_agents.exs
- backend/lib/backend/agents/agent.ex
- backend/lib/backend/agents.ex
- backend/test/backend/agents_test.exs

Migration Result:
  mix ecto.migrate → "Migrations already up" (agents table confirmed in DB)
  Verified via: SELECT table_name FROM information_schema.tables WHERE table_name='agents' → 1 row

Test Results:
  mix test test/backend/agents_test.exs → 19 tests, 0 failures
  mix test (full suite)                 → 50 tests, 0 failures

Tests Cover:
  1. Valid changeset with name and status
  2. Changeset rejects missing name
  3. Changeset rejects invalid status ("busy")
  4. Changeset accepts all valid statuses (idle, working, offline, error)
  5. create_agent/1 creates with default status "idle" and capabilities ["general"]
  6. create_agent/1 with custom capabilities
  7. create_agent/1 returns error on missing name
  8. create_agent/1 returns error on invalid status
  9. list_agents/0 returns all agents
  10. list_agents/0 returns empty list when none exist
  11. get_agent!/1 returns agent by UUID
  12. get_agent!/1 raises Ecto.NoResultsError for unknown id
  13. update_agent/2 updates fields
  14. update_agent/2 returns error on invalid update
  15. delete_agent/1 removes agent from database
  16. get_idle_agents/0 returns only idle agents
  17. get_idle_agents/0 excludes non-idle agents
  18. stop_agent/1 sets status to offline, clears current_task_id and terminal_pid
  19. stop_agent/1 on idle agent sets it to offline

Git Commit:
  7bded70 feat(backend): add agent schema, migration, and tests
  Branch: elixir-backend
